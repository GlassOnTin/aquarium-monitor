# Direct dependencies. Transitive deps resolve fresh on rebuild so a future
# Python upgrade can pull matching wheels. plotly is browser-side (CDN), not here.
Flask==3.1.3
pandas==3.0.3
requests==2.34.2
tinytuya==1.18.1
openpyxl==3.1.5
